import { SparkleIcon } from '@/components/SparkleIcon'
import { generateRssFeed } from '@/lib/generateRssFeed'

export async function getStaticProps() {
  if (process.env.NODE_ENV === 'production') {
    await generateRssFeed()
  }
  return { props: {} }
}

---

## [Kubernetes] Kubernetes HPA with monitoring system {{ date: '2023-09-09T00:00Z' }}

the ideas to create Horizontal Pod Autoscaling based on CPU utilization with <b>Prometheus</b> and <b>Grafana</b> to continously monitoring cluster.
This project aims to fulfill final assignment, it might be a lot of misconception about everything and it might be not a best practice to do.
im still working and learn on this project to make it perfect.

![HPA High level architecture](src/images/HPA.jpg)

[Source](https://github.com/kurniawaanr/HPA.git)

---

## [Terraform] Azure Kubernetes Service with Terraform {{ date: '2024-03-10T00:00Z' }}

The plan involves leveraging Terraform to provision an Azure Kubernetes Service (AKS) and dig deeper into Infrastructure as Code (IaC).
This initiative aims to enhance skills in both Terraform and cloud-native infrastructure management by deploying AKS on Azure through code-based configuration.
On this project, I can gain proficiency in IaC principles and understand the intricacies of managing AKS clusters in the Azure ecosystem.

```json
ON PROGRESS
```

---

## [AWS] Deploying static website using Amazon S3 Bucket {{ date: '2024-03-20T00:00Z' }}

This project focuses on deploying a static website using Amazon Simple Storage Service (S3) bucket, a highly scalable and cost-effective storage solution provided by Amazon Web Services (AWS).
Deploying static websites to S3 offers simplicity, reliability, and performance, making it an ideal choice for hosting static content such as HTML, CSS, JavaScript files, and images.

```json
ON PROGRESS
```

---

## [Jenkins] CI/CD pipeline for NodeJS using Jenkins and Docker {{ date: '2024-04-15T00:00Z' }}

This project aims to set up a Continuous Integration/Continuous Deployment (CI/CD) pipeline for a Node.js application using Jenkins and Docker.
By automating the build, test, and deployment processes, developers can ensure code quality, streamline development workflows, and deliver software more efficiently.

```json
ON PROGRESS
```

---

## My Next Projects Plan {{ date: '2024-05-01T00:00Z' }}

> [ExpressJS] Invoicing App using Express JS <br></br> [ExpressJS] E-commerce Back-end using Express JS <br></br> [Kubernetes] Blue-Green deployment with Kubernetes
